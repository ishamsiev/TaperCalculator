Put PNG/JPG pictures of your extension here — a dialog, the toolpath it makes, the toolbar button.

They travel inside the package: the store lists them as the card's screenshots and uses the first one
as the card cover, so a card with pictures needs no image hosting anywhere.

Only image files are copied into the package, so this note stays out of it.
